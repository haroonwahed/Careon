# Copy to .env.e2e (optional). Playwright loads .env.e2e when present — values do not override
# variables already set in your shell.
#
# Canonical rehearsal pilot stack (must match ./scripts/prepare_pilot_e2e.sh + seed_pilot_e2e):
E2E_BASE_URL=http://127.0.0.1:8010
E2E_DEMO_PASSWORD=pilot_demo_pass_123
E2E_SMOKE_PASSWORD=e2e_pass_123

# Golden path uses provider two (Kompas) — defaults match seed when unset:
# E2E_PROVIDER_TWO_USERNAME=demo_provider_kompas
