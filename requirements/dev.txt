-r runtime.txt

# Local dev UX
django-browser-reload==1.18.0
django-debug-toolbar==4.4.6

# Git hooks / terminology enforcement
pre-commit

# Type stubs for Django (reduces Pylance false positives)
django-stubs[compatible-mypy]

# Python type checker — run as `pyright` in CI (pyrightconfig.json at repo root)
pyright==1.1.401

# Pytest (local + CI: `python -m pytest` from an env with `pip install -r requirements/dev.txt`)
pytest==8.3.5
pytest-django==4.10.0
