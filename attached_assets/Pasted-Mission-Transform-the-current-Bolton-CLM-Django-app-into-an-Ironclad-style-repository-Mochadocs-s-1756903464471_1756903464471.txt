Mission: Transform the current Bolton CLM Django app into an Ironclad-style repository + Mochadocs-style document system while preserving all existing routes, forms, and data.
NO breaking changes: don’t rename or remove current views, URLs, models, or templates unless explicitly stated. Replicate behaviors and visual patterns, not proprietary code.

0) Safety & Branching

Create a new branch: feat/ironclad-mochadocs-ui.

Snapshot current state: git add -A && git commit -m "chore: snapshot before Ironclad+Mochadocs revamp".

Add feature flags (core/flags.py):

IRONCLAD_MODE = True    # advanced repository UX
MOCHADOCS_MODE = True   # templates/clauses/merge/obligations
TEST_MODE = True        # mocks for e2e


Import flags where needed; all new behavior must be guarded by these booleans. With flags False, app must behave exactly as today.

1) Design System (Bolton look, Ironclad layout)

Keep Bolton brand but adopt Ironclad’s neutral vibe (soft borders, airy surfaces, filter chips, underlined active tabs).

Create static/css/theme.css with CSS variables:

:root{
  --primary-700:#123e9e; --primary-600:#174cbc; --primary-500:#1e5bdc;
  --accent-500:#f2c100;  /* yellow */
  --bg:#f4f7fb; --card:#fff; --border:#e6eaf1; --muted:#667085;
  --success:#0f766e; --warning:#b45309; --danger:#b91c1c;
  --radius-xl:16px; --radius-2xl:20px; --shadow-sm:0 1px 2px rgba(16,24,40,.06);
  --ring:0 0 0 2px rgb(30 91 220 / .35);
}
html,body{background:var(--bg);}


Ensure Tailwind builds include theme.css. Use utility helpers in static/css/base.css:

.card{background:var(--card);border:1px solid var(--border);border-radius:var(--radius-xl);box-shadow:var(--shadow-sm)}
.pill{border-radius:9999px}
.focus-ring:focus{outline:none;box-shadow:var(--ring)}


Update base.html: top tabs bar (Dashboard, Contracts, Repository, Workflow Designer, Insights, Obligations, Entities), left sidebar scaffold, and global search slot.

2) Reusable UI Partials (Django templates)

Create these includes under templates/ui/ and refactor existing pages to use them:

top_tabs.html – tabs with thin 2px underline for active tab.

sidebar_repo.html – Views / Workflows / Repository / My Views with counts (Ironclad pattern).

chip.html – filter chip (inactive: border pill; active: filled primary-50).

badge.html – status badges: Draft, Active, Inactive, Unverified.

toolbar.html – filters row: Stage/Status, Type, Counterparty, People, Date, Save View, Search, “New” menu.

list_row.html – contract row (left: title/counterparty; right: status badge + hint like “Review” / “Expiring • 60d”).

drawer.html – right-side details drawer (HTMX target) with deep-link ?contractId=....

kpi_card.html – small KPI pill cards for Dashboard.

Ensure HTMX attributes (hx-get, hx-target, hx-swap, hx-push-url) are used for all filter/search updates.

3) Repository UX (Ironclad-like)

View: contracts/views_repository.py: repository(request) using query params q, status[], type[], people[], counterparty[], date_from, date_to, sort, page, size.

Template: contracts/repository.html using the partials from §2.

Features:

Filter chips (multi-select) + Clear.

Saved Views (star/rename/delete) in model SavedView(user, name, querystring); show under My Views in the sidebar.

Bulk selection bar (sticky when rows selected): actions Change status, Assign to me, Export CSV.

Right-side Drawer opens via row click → contracts/drawer/<id>/ returns partial; Esc closes; URL sync with ?contractId=.

Search (debounced) and sort (Updated desc, Title, Status).

Pagination 25/50/100.

Keyboard shortcuts: / focus search, n new contract, esc close drawer, shift+a select all page rows.

Role gating: viewer/editor/admin; disable bulk actions for viewer (server checks too).

Keep existing models; add lightweight fields only if truly needed (with migrations).

4) Mochadocs-style Docs

Pages & URLs (guarded by MOCHADOCS_MODE):

/templates/ – list, create, edit (rich text or Markdown area), tags, version notes.

/clauses/ – clause library with tags and search; Insert into template via modal.

/contracts/<id>/generate/ – generate from template + variables form; preview page with merge variables.

/obligations/ – table of key dates/owners/status; show timeline widget on Dashboard.

Minimal models if missing: Template, Clause, Obligation, DocumentVersion; all linked to existing Contract when appropriate.

HTMX flows: insert clause, save template version, generate contract → create a DocumentVersion and attach to contract (stub file field OK).

Activity log per contract (simple ContractLog model) and show in drawer.

5) Services & Adapters (non-breaking)

Keep existing views; add service functions services/repository.py, services/templates.py etc., called by views.

If any data comes later from an API, keep the same service interface; for now, use ORM.

Add export_csv(request) for visible rows.

Add bulk_update_status(ids, status); optimistic UI with HTMX indicators.

6) Integrations Page (look & cards like Ironclad)

/integrations/ with card grid: Box, Dropbox, Google Drive, OneDrive, Slack, Salesforce, SAML, etc. Cards show “Explore” or “Configured” status (mocked).

Keep cards purely presentational for now (links to “coming soon” modals).

7) Styling specifics (to truly look right)

Page container: max-w-7xl mx-auto px-6.

Cards: .card + rounded-2xl on big panels.

Active tab underline (2px --primary-600).

Buttons:

Primary (blue): rounded-full, bg --primary-700, white text, shadow, hover darken, focus ring --ring.

Accent (yellow) for main CTAs like New.

Filter chips: pill; active → subtle blue fill + primary text; inactive → white + border.

Dots in “Stage” (like Ironclad) can be small circles using bg-green-500/gray-300 inline.

8) Templates cleanup (fix Django errors)

Scan templates/** for invalid {% else %} after {% for %} and replace with {% empty %}.

Ensure every {% if %} has {% endif %}, every {% for %} has {% endfor %}.

Run python manage.py check and fix all admin.E108 by adding @admin.display helpers rather than removing columns.

9) Tests & Parity Audit

Add Django tests for repository filters, saved views, bulk actions, drawer route, export CSV, template → clause insert → version save, obligations timeline.

Add Playwright (Python) e2e (headless) with TEST_MODE=True:

Visit /contracts → use chips/search/sort/pagination → open drawer → change status → close.

Bulk select → assign → export CSV.

New Contract flow (basic form) → lands in drawer.

/templates create → insert clause → save version.

/obligations create → appears on Dashboard timeline.

Create parity/targets.yml describing the Ironclad/Mochadocs behaviors implemented.

Add scripts/gap_audit.py that loads targets.yml, checks for routes, templates/partials, and runs a few template renders to confirm UI nodes exist; output to parity/parity-report.md.

NPM/Poetry not used; stick to Python tooling: pytest, pytest-django, playwright.

Add scripts in Makefile (or shell scripts)

check: ; python manage.py check
test: ; pytest -q
e2e: ; pytest -q tests/e2e
audit: ; python scripts/gap_audit.py
verify: check test e2e audit

10) Acceptance Criteria (what you must show)

With both flags False → app looks/works exactly as before.

With flags True →

Repository has chips, saved views, drawer, bulk bar, keyboard shortcuts, export CSV, pagination, counts in sidebar.

Mochadocs features (templates, clauses, generate, obligations) present and usable.

Integrations page with cards.

All templates render; python manage.py check passes; no admin.E108.

make verify passes; attach parity/parity-report.md.

Commit with message: feat: Ironclad-style repository + Mochadocs docs system (flagged, non-breaking) and provide a short summary of changed files with line counts.

11) Run & Demo

Kill any old servers using port; start one clean instance:

pkill -f "manage.py runserver" || true
python manage.py runserver 0.0.0.0:$PORT --noreload


Seed minimal data if empty (10–20 contracts with mixed statuses; a few templates/clauses/obligations).

Provide screenshots/GIFs of: Repository (chips+bulk bar), Drawer, New menu, Templates/Clauses flow, Obligations timeline, Integrations page, plus the final parity report.

Important: Do not copy Ironclad/Mochadocs code or assets; match layout patterns and behaviors only. Keep every change behind flags and leave current auth templates and routes intact.