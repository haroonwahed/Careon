Goal: Make the dashboard UI match the Ironclad-style screenshot (dense table, subtle grays, green status pills, slim left nav, toolbar with search + Filter + Columns) by styling only. Do not change Python views, URLs, models, or API logic.
Scope: HTML templates + CSS/Tailwind only. If a class rename is unavoidable, keep the old class as a data attribute data-legacy-class="<old>".

Guardrails (must follow)

No backend edits (contracts/views.py, urls.py, models) — style only.

No JS logic changes; UI behavior must stay identical.

Put all new styles in a separate theme file and activate them via a body class: <body class="theme-iron">.

Add an easy revert: removing theme-iron restores the old look.

Files to add/change

static/css/theme-iron.css (or static/css/theme-iron.scss if using SCSS)

Update base layout template (e.g. templates/base.html) to link the file and add theme-iron to <body> only if IRON_THEME context var is true. If that var doesn’t exist, hardcode the class for now.

Design tokens (approximate from screenshot)

Create CSS variables so everything is themable:

/* static/css/theme-iron.css */
:root.theme-iron {
  --bg: #f6f7f9;
  --panel-bg: #ffffff;
  --border: #e5e7eb;
  --muted: #6b7280;
  --text: #111827;
  --text-soft: #374151;
  --primary: #111827;           /* Top nav/icons mostly dark */
  --accent: #2563eb;            /* Action blue buttons */
  --success: #16a34a;           /* Green status pills */
  --hover: #f3f4f6;

  --radius: 12px;
  --radius-sm: 8px;
  --shadow: 0 1px 2px rgba(0,0,0,.06), 0 8px 24px rgba(0,0,0,.04);

  --table-row-h: 44px;
  --table-font: 14px;
  --btn-h: 36px;
}

Layout + components
/* Page background + cards */
.theme-iron body { background: var(--bg); color: var(--text); }
.theme-iron .card, .theme-iron .panel { background: var(--panel-bg); border: 1px solid var(--border); border-radius: var(--radius); box-shadow: var(--shadow); }

/* Top actions */
.theme-iron .btn-primary { background: var(--accent); color:#fff; border-radius: 10px; height: var(--btn-h); padding: 0 14px; }
.theme-iron .btn-outline { background: #fff; border: 1px solid var(--border); color: var(--text); border-radius: 10px; height: var(--btn-h); }

/* Left nav - slim */
.theme-iron .sidebar { width: 220px; border-right: 1px solid var(--border); background: #fff; }
.theme-iron .sidebar .section-title { color: var(--muted); font-weight: 600; font-size: 12px; text-transform: uppercase; }
.theme-iron .sidebar a.item { display:flex; align-items:center; gap:10px; padding:8px 12px; border-radius: 8px; color: var(--text-soft); }
.theme-iron .sidebar a.item.active,
.theme-iron .sidebar a.item:hover { background: var(--hover); color: var(--text); }

/* Toolbar (search + Filter + Columns) */
.theme-iron .toolbar { display:flex; gap:10px; align-items:center; }
.theme-iron .toolbar .search { max-width: 320px; }
.theme-iron .toolbar .search input { height: 36px; border:1px solid var(--border); border-radius: 10px; padding: 0 12px; }

/* Table styles - compact, zebra hover */
.theme-iron table.data { width:100%; border-collapse: separate; border-spacing:0; font-size: var(--table-font); }
.theme-iron table.data thead th { text-align:left; color: var(--muted); font-weight:600; padding: 10px 12px; border-bottom:1px solid var(--border); background:#fff; position: sticky; top:0; }
.theme-iron table.data tbody tr { height: var(--table-row-h); border-bottom:1px solid var(--border); }
.theme-iron table.data tbody tr:hover { background: var(--hover); }
.theme-iron table.data td { padding: 8px 12px; color: var(--text-soft); }

/* Status pills */
.theme-iron .pill { display:inline-flex; align-items:center; gap:6px; padding:4px 10px; border-radius:999px; background:rgba(22,163,74,.08); color: var(--success); border: 1px solid rgba(22,163,74,.2); font-weight:600; }

/* Quick stats */
.theme-iron .quick-stats .stat { display:flex; justify-content:space-between; padding:8px 12px; border:1px solid var(--border); border-radius:10px; background:#fff; }

/* Empty state */
.theme-iron .empty { text-align:center; color: var(--muted); padding:40px 0; }
.theme-iron .empty a { color: var(--accent); font-weight:600; }

Minimal HTML class hooks (don’t rename existing IDs)

Update templates to add hooks without breaking selectors:

<div class="layout theme-layout">
  <aside class="sidebar">
    <div class="section-title">My Views</div>
    <a class="item active" href="#">In Progress</a>
    <!-- ... -->
  </aside>

  <main class="panel">
    <div class="toolbar">
      <button class="btn-primary">Start Workflow</button>
      <div class="search"><input type="search" placeholder="Search for a workflow"></div>
      <button class="btn-outline">Filter 2</button>
      <button class="btn-outline">Columns</button>
    </div>

    <table class="data">
      <thead>
        <tr>
          <th>Workflow Name</th><th>Stage</th><th>Agreement Date</th><th>Region</th><th>Projected Spend</th>
        </tr>
      </thead>
      <tbody>
        <!-- existing rows; no logic change -->
        <tr>
          <td>Artist Licensing Agreement with …</td>
          <td><span class="pill">● Review</span></td>
          <td>January 26, 2023</td>
          <td>United States</td>
          <td>$39,093,592.00</td>
        </tr>
      </tbody>
    </table>

    <div class="empty" data-when="no-contracts">No contracts found. <a href="#">Create your first contract</a></div>
  </main>
</div>

Acceptance tests (must still pass)

All routes, filters, and “Start Workflow / New Contract” buttons keep the same hrefs and onClick handlers.

Table column visibility and sorting behave exactly as before.

“No contracts found” state still renders conditionally (CSS only).

Mobile: table scrolls horizontally with sticky headers; nothing overlaps.

What to change in code, step-by-step

Add static/css/theme-iron.css with the CSS above.

In base.html (or your main layout), include the stylesheet and add class="theme-iron" on <body>.

In the dashboard template, add the lightweight class hooks (sidebar, toolbar, data, pill, etc.) without removing existing classes.

Do not modify JS; if a selector depends on an old class, keep it (use data-legacy-class if needed).

Run and verify visually against the Ironclad screenshot.

Non-negotiables

Don’t touch Python.

Don’t rename existing form or table IDs.

No package upgrades.

If a required class hook collides, add a wrapper <div> instead.