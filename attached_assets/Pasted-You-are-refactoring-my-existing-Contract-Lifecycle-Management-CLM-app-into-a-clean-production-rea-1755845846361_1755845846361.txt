You are refactoring my existing Contract Lifecycle Management (CLM) app into a clean, production‑ready, multi‑tenant SaaS comparable to Ironclad and MochaDocs. Use the complete blueprint below as the scope and architecture. Process it sequentially: start at section #1 and complete it fully; then stop, show me the changes (files, diffs, notes, and how to run), and wait for my OK before proceeding to the next numbered section. Do not jump ahead. Do not remove working code.

Execution rules

Maintain current features. Add changes behind a feature flag FEATURE_REDESIGN=true.

For each section you complete: create tests, run lint/typechecks, and give me exact run commands.

If something is ambiguous, make a pragmatic choice and document it in a short DECISIONS.md entry.

0) Non‑negotiables (use as guardrails; do not implement yet)

Preserve existing routes & APIs; new code behind FEATURE_REDESIGN=true.

Add/adjust automated tests for anything touched.

Tech: React + Tailwind (or current stack), small internal design system.

Include minimal seed/demo data so the new UX is visible immediately.

1) Brand system (Bolton style) — START HERE

Create a unified theme and component primitives.

Tailwind tokens

--bg: #FFFFFF

--ink: #0B0B0C

--muted: #6B7280

--stroke: #E5E7EB

--accent: #0E9F6E

--warn: #F59E0B

--danger: #DC2626

--card: #FFFFFF
Typography: Inter; base 14/16, section headers 24/28, page titles 32/36.

Build a small design system
Container, PageHeader, Card, Stat, Table, Badge, Button (primary/secondary/ghost/destructive), Input, Select, DatePicker, Modal, Drawer, Tabs, EmptyState, Toast, Stepper, Progress.

Deliver:

Updated Tailwind config + CSS variables.

Reusable components with stories or a quick /components-demo route.

Snapshot/unit tests for components.

Stop and show me results.

2) Global layout & navigation

Sticky top nav: Logo (Bolton CLM), global search, + New Contract CTA, user menu.

Collapsible left dock: My Views (Dashboard, All Contracts, Legal Tasks, Active Workflows), Quick Actions, Filters.

Content grid with max‑width 1200px.

Keyboard shortcuts: / focus search; N new contract; G then C/T/R/W for section nav.

Implement feature flag switch to toggle old/new layout.

Deliver a working shell that wraps existing pages. Stop and show me.

3) Dashboard

Four Stat cards: Total Contracts, Pending Tasks, Active Workflows, Expiring Soon.

Recent activity feed (contracts, comments, approvals).

Quick Actions: New Contract, Start Workflow, Import, Upload.

Saved filters chip row (interactive).

Seed demo data to render. Tests for stat queries and empty states.

4) Contracts (index)

Table columns: Title, Counterparty, Type, Jurisdiction, Value, Status, Owner, Next Milestone, Updated.

Server‑side filtering: status, tags, owner, date range, value range.

Bulk actions (tag, assign, archive).

Right‑panel preview drawer on row click.

URL‑synced filters (/contracts?status=Active&owner=me&tag=NDA).

“Save view” chips.

Add API handlers if missing; tests for filtering & pagination.

5) Contract create/edit

Stepper flow: 1) Overview 2) Parties 3) Terms & Dates 4) Files 5) Approval & Signature.

Required‑field validation, autosave, leave‑page guard.

Attachments: drag‑drop, MIME whitelist, size cap; version label.

On success, route to contract detail with toast.

E2E test for the full creation path.

6) Workflow Designer

Index table + “Templates” drawer.

Template editor: linear stages with schema
{ name, requiredFields[], approvers[], slaDays, onEnter[], onExit[] }.

Instance progress bar; instance drawer shows current stage, blockers, comments.

Enforce required fields before advancing.

Unit tests for the state machine; E2E for start→advance→done.

7) Legal Tasks

Kanban (Backlog/In Progress/Blocked/Done) + table toggle.

Task drawer: linked contract(s), due date, owner, priority, audit.

Drag‑to‑move with optimistic UI.

Tests for board transitions and permissions.

8) Repository

Folder tree + search facets (doc type, counterparty, jurisdiction, year, tags).

Versioning: bump on re‑upload; show version history; download old versions.

“Compare versions” placeholder (metadata diff).

API + tests for versioning and listing.

9) Risks / Compliance / M&A Due Diligence / Budgets / Trademarks

Implement clean index tables + detail drawers using the same table/drawer pattern.

Shared filter bar; URL‑synced state.

Seed minimal demo data for each module.

10) Data model (multi‑tenant)

Add org_id to multi‑tenant tables; enforce row‑level scoping in APIs.

Tables:

organizations(id, name, domain, plan, created_at)

users(id, org_id, name, email, role, status)

roles(id, org_id, name) + role_permissions(role_id, permission)

contracts(id, org_id, title, counterparty_id, type, jurisdiction, value, status, owner_id, milestone_date, tags[], created_at, updated_at)

counterparties(id, org_id, name, contact_email, region, risk_score)

workflows(id, org_id, template_id, contract_id, stage, progress, started_at, due_at, owner_id, status)

workflow_templates(id, org_id, name, json_schema)

tasks(id, org_id, title, description, status, assignee_id, due_at, contract_id, workflow_id, priority)

documents(id, org_id, contract_id, filename, version, storage_url, hash, uploaded_by, uploaded_at)

comments(id, org_id, entity_type, entity_id, author_id, body, created_at)

audit_logs(id, org_id, actor_id, action, entity_type, entity_id, before, after, at)

tags(id, org_id, name, color)

alerts(id, org_id, kind, entity_type, entity_id, due_at, resolved_at)

Create migrations + seed script. Tests for org scoping.

11) RBAC & security

Roles: Admin, Legal, Business Owner, Viewer.

Permissions: contracts.read/write/approve/delete, workflows.manage, tasks.manage, repo.read/write, admin.users, etc.

Middleware for org + permission gates on every API route.

Soft delete via deleted_at.

Full audit logs for create/update/delete/approve.

API tests for permission matrices.

12) APIs (REST; GraphQL‑ready)

/api/contracts (list, filter, create, update, archive, bulk‑update)

/api/workflows (list, start, advance, block/unblock)

/api/tasks (list, move, assign, complete)

/api/documents (multipart upload, version, list)

/api/search (contracts/tasks/docs unified)

/api/alerts (expiring, approvals)

All with pagination, sorting, filter params, org_id enforcement. Add OpenAPI spec.

13) Search & filters

Text index on Title/Counterparty/Tags + facets.

Debounced query; virtualize tables over 100 rows.

Save/recall filter sets per user.

Tests for facet combos and URL syncing.

14) Documents

Pluggable storage adapter: local now; S3‑compatible later (clean interface).

MIME whitelist, 50MB cap, virus/mime check stub.

Version bump + download history.

15) UX polish

Replace oversized hero icons with balanced Stat cards (icon left, bold number, small label).

Spacing on 8px scale; zebra rows optional.

Empty states with single CTA; skeleton loaders; optimistic updates.

A11y: focus rings, roles, labels, AA contrast.

16) Internationalization (NL default)

i18n scaffold with en and nl namespaces (Contracts, Jurisdictie, Taken, etc.).

Provide a language switcher in user menu.

17) Testing & quality gates

Unit tests for utils/reducers.

API tests: auth, org scoping, permissions.

E2E (Playwright) critical flows:

Create contract → upload doc → start workflow → advance stages → done.

Create task from contract; Kanban move.

Filters persist via URL; saved view appears on dashboard.

CI: lint, typecheck, tests on every push.

18) Performance

Route‑level code‑splitting; cache list queries.

Debounce search; virtualization for large tables.

19) Telemetry

Minimal event logger: page_view, contract_created, workflow_advanced.

Log to console with TODO to wire analytics later.

20) Deliverables

Tailwind theme + component library.

Refactored layout and the pages listed above.

API endpoints & migrations for new schema.

Seed data for a demo org with sample contracts/workflows/tasks.

Tests (unit/API/E2E).

Short README: how to run, toggle FEATURE_REDESIGN, demo accounts.

Remember: start with #1 Brand system, complete it, show diffs and run steps, then await my confirmation before moving to #2.