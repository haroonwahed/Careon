Context: You are refactoring my existing Bolton CLM app (Django back‑end + Tailwind/Vite front‑end) to ensure it runs correctly on Replit and meets production best practices. The source code is already available in the current repository; do not recreate it from scratch.
Goal: Fix build/runtime errors, clean up configuration, and implement the recommendations below. Work in small, verifiable steps; show diffs and test commands after each task.

1. Configure Tailwind & PostCSS

The project uses ES modules ("type":"module" in package.json), but tailwind.config.js and postcss.config.js are written in CommonJS. EITHER:

Rename them to tailwind.config.cjs and postcss.config.cjs, OR

Convert them to ES modules with export default { … } and import plugins at the top.

Remove any unused plugins from tailwind.config.* (e.g. forms/typography) unless they’re actually used. If they are needed, add them to devDependencies and require/import them properly.

Simplify postcss.config.* to use only tailwindcss and autoprefixer. You can drop the deprecated @tailwindcss/postcss shim.

2. Fix Vite config for Replit

Edit vite.config.js so the dev server accepts Replit subdomains:

server: {
  host: '0.0.0.0',
  port: 5173,
  strictPort: false,
  allowedHosts: ['.replit.dev', '.repl.co'], // or 'all' if supported
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
      secure: false,
    },
  },
}


Keep base: '/' so assets load correctly when served behind Django.

3. Harden Django settings

Do not hardcode SECRET_KEY in version control. Read it from an environment variable (e.g. os.environ.get('SECRET_KEY', 'fallback')).

Set ALLOWED_HOSTS to ['.replit.dev', '.repl.co'] and move this to an environment variable for production.

For a Dutch deployment, change LANGUAGE_CODE = 'nl' and TIME_ZONE = 'Europe/Amsterdam'. Keep USE_TZ = True.

Move DEBUG to an environment variable so you can toggle it off in production.

4. Update .replit and project scripts

Ensure .replit runs both the Django server and the Tailwind build. One approach:

entrypoint = "manage.py"
[env]
PORT = "8000"
run = """
pip install -r requirements.txt
cd theme/static_src && npm install
cd .. # back to project root
concurrently -k "cd theme/static_src && npm run dev" "python manage.py migrate --noinput && python manage.py runserver 0.0.0.0:$PORT"
"""


(Install concurrently as a dev dependency in theme/static_src/package.json.)

Confirm the Tailwind watcher writes CSS to theme/static/css/dist/styles.css and that STATICFILES_DIRS in settings.py points to this directory.

5. Add Dutch locale and adjust pages

Internationalize your templates ({% trans %} tags) and add Dutch translations in locale files.

Adjust template text (e.g. “Dashboard”, “Transacties”, “Kilometers”) to Dutch.

6. Testing & cleanup

Add a smoke test (e.g. Playwright) that:

Starts the server and checks the Dashboard loads without console errors.

Navigates through each module (Transacties, BTW, Kilometers, Belasting, Assistent, Aftrekposten) and confirms pages are scrollable and buttons open modals/drawers.

Update README.md with clear instructions:

pip install -r requirements.txt

cd theme/static_src && npm install

.replit run command or manual commands.

Document any remaining assumptions in docs/DECISIONS.md.